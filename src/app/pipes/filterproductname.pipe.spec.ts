import { FilterproductnamePipe } from './filterproductname.pipe';

describe('FilterproductnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterproductnamePipe();
    expect(pipe).toBeTruthy();
  });
});
